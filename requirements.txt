# Runtime dependencies, fully pinned for reproducible, auditable builds.
# Pinning exact versions means `pip-audit` checks the SAME versions that ship,
# and a surprise upstream release can't silently change behavior under us.

# --- Direct dependencies ---
Django==5.2.15            # latest stable; LTS line
argon2-cffi==25.1.0       # Argon2 password hashing
django-axes==8.3.1        # brute-force lockout
django-csp==4.0           # Content-Security-Policy header
django-environ==0.14.0    # 12-factor config from env vars
psycopg[binary]==3.3.4    # PostgreSQL driver (prod DB; SQLite needs no driver)

# --- Transitive dependencies (pinned for reproducibility) ---
argon2-cffi-bindings==25.1.0
asgiref==3.11.1
cffi==2.0.0
psycopg-binary==3.3.4
pycparser==3.0
sqlparse==0.5.5
typing_extensions==4.15.0
